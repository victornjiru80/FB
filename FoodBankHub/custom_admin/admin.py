from django.contrib import admin

# Register your models here.
# This file is intentionally left empty as we're using custom views
# instead of Django's default admin interface for the custom_admin app
